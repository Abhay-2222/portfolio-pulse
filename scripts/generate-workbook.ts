/**
 * Deprecated: Portfolio Pulse Phase 1 now uses the real Google Sheet export
 * at data/Enterprise_Portfolio_Data.xlsx (copied from the provided Drive workbook).
 *
 * Do not regenerate a synthetic workbook — keep the provided file in sync instead.
 */
console.error(
  "generate-workbook is disabled. Use data/Enterprise_Portfolio_Data.xlsx (real Google Sheet export).",
);
process.exit(1);
